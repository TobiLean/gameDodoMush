/// @function screenShake(magnitude,frames)
/// @desc poopoo
/// @param {real} Magrenitude sets the strength of the shake (radius in pixels)
/// @param {real} Frames sets the length of the shake in frames (60 = 1 at 60fps)

function screenShake(Magnitude, Frames)
{
	with (objCamera)
	{
		if (argument0 > shakeRemain)
		{
			shakeMag = argument0;
			shakeRemain = argument0;
			shakeLength = argument1
	
		}

	}
}